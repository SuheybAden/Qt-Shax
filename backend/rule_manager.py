import numpy as np

# Checks if a move violates any rules
def isValidMove(old_x, old_y, new_x, new_y, board_state):
    
    return True

def getPossibleMoves(piece_x, piece_y, board_state:np.ndarray):
    # Stores all the possible moves of the piece
    # 0 means a piece can't move to that index
    # 1 means a piece can move to that index
    possibleMoves = np.zeros(board_state.shape)
    return possibleMoves

def findJare(board_state):
    return False